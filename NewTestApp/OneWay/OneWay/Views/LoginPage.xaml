<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="OneWay.Views.LoginPage">
    <ContentPage.Content>
        <StackLayout HorizontalOptions="Center" VerticalOptions="Center" Orientation="Horizontal">
            <Label Text="Username :" HorizontalOptions="Center" VerticalOptions="CenterAndExpand" 
                   FontSize="Micro"/>
            <Label Text="{Binding LoginModel.UserName}" FontSize="Micro" FontAttributes="Italic" FontFamily="Arial"/>
            <Label Text="Password :" HorizontalOptions="Center" VerticalOptions="CenterAndExpand" 
                   FontSize="Micro"/>
            <Label Text="{Binding LoginModel.Password}" FontSize="Micro" FontAttributes="Italic" FontFamily="Arial"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>