<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ButtonCB.Views.LoginPage">
    <ContentPage.Content>
        <StackLayout HorizontalOptions="Center" VerticalOptions="Center" Spacing="20">
            <Label Text="Simple Exampleof binding buttons" />
            <Entry Placeholder="Username" Text="{Binding User, Mode=TwoWay}"/>
            <Entry Placeholder="Password" Text="{Binding Pass, Mode=TwoWay}"/>
            <Button Text="Login" Command="{Binding LoginCommand}" BackgroundColor="Black" TextColor="White" BorderRadius="20"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>