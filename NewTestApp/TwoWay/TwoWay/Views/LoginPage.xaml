<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TwoWay.Views.LoginPage">
    <ContentPage.Content>
        <StackLayout HorizontalOptions="Center" VerticalOptions="Center" Spacing="8">
            <Label Text="the land of down " FontAttributes="Bold" HorizontalOptions="Center" />
            <Label Text="Forename: 😀 " />
            <Entry Text="{Binding Forename, Mode=TwoWay}" />
            <Label Text="Surname 😀"/>
            <Entry Text="{Binding Surename, Mode=TwoWay}" />
            <StackLayout Padding="0,10,0,0" Orientation="Horizontal">
                <Label Text="ur forename is : "/>
                <Label Text="{Binding Forename}"/>
            </StackLayout>
            <StackLayout Orientation="Horizontal">
                <Label Text="ur surename is : "/>
                <Label Text="{Binding Surename}"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>